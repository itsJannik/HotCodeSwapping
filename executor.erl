-module(executor).

%   1st
%   rpc(Mod, Val) ->

%   2nd / 3rd
%   start(Name, Mod) ->

%   2nd / 3rd
%   rpc(Name, Request) ->

%   2nd / 3rd
%   loop(Name, Mod) ->

%   3rd
%   swap_code(Name, Mod) ->